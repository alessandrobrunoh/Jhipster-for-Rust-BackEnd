# {{ name }}

Generated by **Rhupster** (JHipster for Rust).

## 🚀 Getting Started

### Prerequisites

*   Rust (latest stable)
*   Node.js & npm
*   Docker & Docker Compose (optional)
*   {% if orm == "sqlx" %}sqlx-cli (`cargo install sqlx-cli`){% elif orm == "diesel" %}diesel_cli (`cargo install diesel_cli`){% endif %}

### 🛠️ Backend Setup

1.  Start Database:
    ```bash
    docker-compose up -d
    ```
2.  Run Migrations:
    {% if orm == "sqlx" %}
    ```bash
    sqlx database create
    sqlx migrate run
    ```
    {% elif orm == "diesel" %}
    ```bash
    diesel setup
    diesel migration run
    ```
    {% endif %}
3.  Run Server:
    ```bash
    cargo run
    ```
    The API will be available at `http://localhost:8080`.
    Swagger UI: `http://localhost:8080/swagger-ui`.

### 🎨 Frontend Setup

1.  Navigate to client:
    ```bash
    cd client
    ```
2.  Install Dependencies:
    ```bash
    npm install
    ```
3.  Run Dev Server:
    ```bash
    npm run dev
    ```

## 🏗️ Project Structure

*   `src/domain`: Entity definitions (JHipster Domain).
*   `src/repository`: Data access layer.
*   `src/service`: Business logic & DTOs.
*   `src/web/rest`: REST Controllers (API endpoints).
*   `src/security`: JWT & Auth logic.

## 🔑 Default Credentials

*   **Username:** `admin`
*   **Password:** `admin`
