# {{ name }}

Generated by **Rhupster** (JHipster for Rust).

## 🚀 Getting Started

> 📘 **See [STRUCTURE.md](./STRUCTURE.md) for a detailed architectural deep-dive.**

### Prerequisites

*   Rust (latest stable)
*   Node.js & npm (for frontend)
*   Docker & Docker Compose (optional, for infrastructure)
{% if orm == "sqlx" %}
*   sqlx-cli (`cargo install sqlx-cli`)
{% elif orm == "diesel" %}
*   diesel_cli (`cargo install diesel_cli --no-default-features --features "postgres"`)
{% elif orm == "seaorm" %}
*   sea-orm-cli (`cargo install sea-orm-cli`)
{% endif %}

### 🛠️ Backend Setup (from project root)

1.  Start Database:
    ```bash
    docker compose up -d
    ```
2.  Run Migrations:
    ```bash
    {% if orm == "sqlx" %}
    sqlx database create
    sqlx migrate run
    {% elif orm == "diesel" %}
    diesel migration run
    {% elif orm == "seaorm" %}
    sea-orm-cli migrate up
    {% endif %}
3.  Run Server:
    ```bash
    cargo run -p api
    ```
    The API will be available at `http://localhost:8080`.
    {% if api_ui == "swagger" %}
    Swagger UI: `http://localhost:8080/swagger-ui`.
    {% elif api_ui == "scalar" %}
    Scalar UI: `http://localhost:8080/scalar`.
    {% endif %}

### 🎨 Frontend Setup (from project root)

1.  Navigate to client:
    ```bash
    cd api/client
    ```
2.  Install Dependencies:
    ```bash
    npm install
    ```
3.  Run Dev Server:
    ```bash
    npm run dev
    ```

## 🔑 Default Credentials

*   **Username:** `admin`
*   **Password:** `admin`